---
name: conductor-setup
description: Initialize the Conductor baseline in a repo by creating structured
  context/spec/plan directories and guardrail files.
---

# Conductor Setup

Use this when a repository has no Conductor baseline yet:

```bash
python3 scripts/conductor_tools.py setup
```

Equivalent shell alias if installed globally:

```bash
conductor_setup
```
